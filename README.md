# 使用正则做匹配条件 深度递归

## Install

    npm i regexp-recursion

## Useage

    var recursion = require('regexp-recursion');

    resursion(obj, '.*name.*', function (key, value) { 
        ...
    });
    
